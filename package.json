{
  "name": "org-ethereum-abi",
  "private": true,
  "description": "Portable .cljc Ethereum ABI argument codec — static, dynamic, arrays, tuples",
  "scripts": {
    "test:cljs": "kbb --backend sci --classpath \"$(kbb -A:cljs -Spath)\" scripts/verify-cljs.cljk",
    "vectors": "kbb --backend sci testdata/gen_vectors.cljk > test/ethereum/vectors.cljk"
  },
  "comment": "viem is a test-time oracle only. Nothing in src/ requires it, on either runtime.",
  "devDependencies": {
    "viem": "^2.55.10"
  }
}
